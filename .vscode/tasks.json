{
    "version": "2.0.0",

    "tasks": [
        {
            "label": "manimL",
            "type": "shell",
            "command": "cd ${fileDirname} && manim ${file} Run -o ${fileBasenameNoExtension} -pql",
        },
        {
            "label": "manimH",
            "type": "shell",
            "command": "cd ${fileDirname} && manim ${file} Run -o ${fileBasenameNoExtension} -pqh && read",
        }
    ],
    
}